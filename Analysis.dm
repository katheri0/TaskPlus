 **Next Steps :**
1. **Dynamic Props:** Ensure that components like `TaskCard.vue` are fully data-driven by passing task data as `props` from the parent views, replacing any mock/hardcoded HTML.
2. **Implement Testing:** Add `Vitest` to the project to write unit tests for the complex logic inside `TasksStore.js` and `utils/storage.js`.
3. **TypeScript Migration:** Consider migrating to TypeScript for better developer experience and type safety as the data models (Tasks, Filters) grow more complex.

